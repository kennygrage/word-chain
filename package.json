{
  "name": "word-chain",
  "version": "1.0.0",
  "description": "Accepts start and end words, and builds a word chain between them.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Kenny Grage",
  "license": "ISC",
  "dependencies": {
    "inquirer": "^5.2.0",
    "request": "^2.86.0"
  }
}
